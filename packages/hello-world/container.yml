name: hello_world
version: 1.0.0
kind: ecu

files:
  - hello.sh

entrypoint:
  kind: task
  exec: hello.sh

actions:
  'hello_world':
    command:
    input:
    output:
    - name: output
      type: string